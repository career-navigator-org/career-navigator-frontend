import { ReactComponent as LogoutIcon } from "../icons/logoutIcon.svg";

export const LogoutIconComponent = ({ className, style }) => {
    return <LogoutIcon className={className} style={style} />
};
