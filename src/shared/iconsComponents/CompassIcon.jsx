import { ReactComponent as CompassIcon } from "../icons/compassIcon.svg";

export const CompassIconComponent = ({ className, style }) => {
    return <CompassIcon className={className} style={style} />
};
